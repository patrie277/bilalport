const nav=document.querySelector('#nav'),toggle=document.querySelector('.menu-toggle');
toggle.addEventListener('click',()=>{const open=nav.classList.toggle('open');toggle.setAttribute('aria-expanded',open)});
document.querySelectorAll('nav a').forEach(a=>a.addEventListener('click',()=>nav.classList.remove('open')));
const links=[...document.querySelectorAll('nav a[href^="#"]')];
const observer=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting)links.forEach(a=>a.classList.toggle('active',a.getAttribute('href')==='#'+e.target.id))}),{rootMargin:'-35% 0px -55% 0px'});
document.querySelectorAll('main section[id]').forEach(s=>observer.observe(s));
document.querySelectorAll('.faq-item').forEach(item=>item.querySelector('button').addEventListener('click',()=>{document.querySelectorAll('.faq-item').forEach(x=>{x.classList.remove('open');x.querySelector('button').setAttribute('aria-expanded','false')});item.classList.add('open');item.querySelector('button').setAttribute('aria-expanded','true')}));
document.querySelector('.faq-item')?.classList.add('open');
const skills=new IntersectionObserver(entries=>entries.forEach(e=>{if(e.isIntersecting){e.target.querySelectorAll('.track i').forEach(i=>i.style.width=i.dataset.level+'%');skills.unobserve(e.target)}}),{threshold:.35});document.querySelectorAll('.skill-card').forEach(s=>skills.observe(s));
const form=document.querySelector('#contact-form'),feedback=document.querySelector('#form-feedback'),toast=document.querySelector('#toast');
form.addEventListener('submit',async e=>{e.preventDefault();feedback.textContent='';feedback.className='';const data=Object.fromEntries(new FormData(form));const errors=[];if(data.nom.trim().length<2)errors.push('Indiquez votre nom.');if(!/^[^@\s]+@[^@\s]+\.[^@\s]+$/.test(data.email))errors.push('Indiquez un email valide.');if(data.sujet.trim().length<3)errors.push('Précisez le sujet.');if(data.message.trim().length<20)errors.push('Le message doit contenir au moins 20 caractères.');if(errors.length){feedback.textContent=errors[0];return}const btn=form.querySelector('button');btn.disabled=true;btn.textContent='Envoi en cours…';try{const res=await fetch('/contact',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify(data)}),json=await res.json();if(!res.ok)throw new Error(json.message);feedback.textContent=json.message;feedback.className='success';form.reset();toast.textContent='✓ '+json.message;toast.classList.add('show');setTimeout(()=>toast.classList.remove('show'),4500)}catch(err){feedback.textContent=err.message||'Impossible d’envoyer le message.'}finally{btn.disabled=false;btn.innerHTML='Envoyer le message <span>→</span>'}});
